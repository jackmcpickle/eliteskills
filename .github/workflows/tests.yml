name: Tests

on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: tests-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: "22"

jobs:
  lint:
    name: Lint & Style
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm run fmt:check
      - run: npx astro sync
      - run: pnpm run lint

  vitest:
    name: Vitest Unit Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm vitest
      - run: pnpm test:cli

  e2e:
    name: Playwright E2E (Human Flow)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Install Playwright browsers
        run: pnpm exec playwright install chromium --with-deps
      - name: Run human checkout e2e
        run: pnpm test:e2e
      - name: Upload Playwright report
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-e2e
          path: playwright-report/
          retention-days: 7

  llm:
    name: LLM Purchase Flow Test
    runs-on: ubuntu-latest
    env:
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
    steps:
      - name: Skip if secret missing
        if: env.ANTHROPIC_API_KEY == ''
        run: |
          echo "::notice::ANTHROPIC_API_KEY not set, skipping"
          exit 0
      - uses: actions/checkout@v4
        if: env.ANTHROPIC_API_KEY != ''
      - uses: pnpm/action-setup@v4
        if: env.ANTHROPIC_API_KEY != ''
      - uses: actions/setup-node@v4
        if: env.ANTHROPIC_API_KEY != ''
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm
      - run: pnpm install --frozen-lockfile
        if: env.ANTHROPIC_API_KEY != ''
      - name: Install Playwright browsers
        if: env.ANTHROPIC_API_KEY != ''
        run: pnpm exec playwright install chromium --with-deps
      - name: Run LLM purchase flow tests
        if: env.ANTHROPIC_API_KEY != ''
        run: pnpm test:llm
        timeout-minutes: 20
      - name: Upload Playwright report
        if: failure() && env.ANTHROPIC_API_KEY != ''
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-llm
          path: playwright-report/
          retention-days: 7

  live-agent:
    name: Live Agent API Smoke Test
    runs-on: ubuntu-latest
    env:
      AGENT_API_KEY: ${{ secrets.AGENT_API_KEY }}
    steps:
      - name: Skip if secret missing
        if: env.AGENT_API_KEY == ''
        run: |
          echo "::notice::AGENT_API_KEY not set, skipping"
          exit 0
      - uses: actions/checkout@v4
        if: env.AGENT_API_KEY != ''
      - uses: pnpm/action-setup@v4
        if: env.AGENT_API_KEY != ''
      - uses: actions/setup-node@v4
        if: env.AGENT_API_KEY != ''
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm
      - run: pnpm install --frozen-lockfile
        if: env.AGENT_API_KEY != ''
      - name: Install Playwright browsers
        if: env.AGENT_API_KEY != ''
        run: pnpm exec playwright install chromium --with-deps
      - name: Run live agent flow tests
        if: env.AGENT_API_KEY != ''
        run: pnpm test:live-agent
        timeout-minutes: 3
      - name: Upload Playwright report
        if: failure() && env.AGENT_API_KEY != ''
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-live-agent
          path: playwright-report/
          retention-days: 7
