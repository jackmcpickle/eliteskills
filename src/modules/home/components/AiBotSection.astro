---
import FeatureCard from '@/components/FeatureCard.astro';
import TerminalBlock from '@/components/TerminalBlock.astro';
import { Bot, Code, ShieldCheck } from '@lucide/astro';

const features = [
	{
		icon: 'Code',
		title: 'CLI Installation',
		description:
			'Install skills directly into your project with a single command. Works with Claude Code, Cursor, Codex, and 40+ more agents.',
	},
	{
		icon: 'ShieldCheck',
		title: 'Open Source',
		description:
			'All skills are MIT licensed and available on GitHub. Browse, fork, and contribute improvements back to the community.',
	},
	{
		icon: 'Bot',
		title: 'Agent-Ready Skills',
		description:
			'Skills are structured for machine consumption. Your AI agents parse, apply, and chain them without manual setup.',
	},
];

const iconMap = { Code, ShieldCheck, Bot };

const agentPrompt = `Search for elite skills that can help with my current task and install them.`;

const codeSnippet = `# Install all skills
npx skills add jackmcpickle/eliteskills

# Install specific skills
npx skills add jackmcpickle/eliteskills --skill elite-react
npx skills add jackmcpickle/eliteskills --skill elite-testing

# List available skills
npx skills add jackmcpickle/eliteskills --list`;
---

<section class="relative py-32 px-6">
	<!-- Ambient glow -->
	<div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 size-[600px] rounded-full bg-neon-violet/5 blur-[140px] pointer-events-none"></div>

	<div class="relative max-w-5xl mx-auto">
		<!-- Section header -->
		<div class="text-center mb-20">
			<p class="reveal font-mono text-xs text-neon-cyan tracking-[0.3em] uppercase mb-4">AI Agent Integration</p>
			<h2 class="reveal reveal-delay-1 font-display text-4xl sm:text-5xl md:text-6xl font-800 text-white tracking-tight">
				Built for Bots, Used&nbsp;by&nbsp;Humans
			</h2>
			<p class="reveal reveal-delay-2 mt-4 max-w-xl mx-auto text-mist">
				Install elite agent skills into any supported coding assistant with a single command.
			</p>
		</div>

		<!-- Two-column layout: features + code -->
		<div class="grid grid-cols-1 lg:grid-cols-2 gap-10 items-start">
			<!-- Feature cards -->
			<div class="space-y-6">
				{features.map((feature, i) => {
					const Icon = iconMap[feature.icon as keyof typeof iconMap];
					return (
						<FeatureCard title={feature.title} description={feature.description} index={i}>
							<Icon slot="icon" class="size-6" aria-hidden="true" />
						</FeatureCard>
					);
				})}
			</div>

			<!-- Code preview -->
			<TerminalBlock label="terminal" class="reveal reveal-delay-2 scroll-border-trace">
				<pre class="p-5 overflow-x-auto font-mono text-xs leading-relaxed text-cloud"><code set:text={codeSnippet} /></pre>
			</TerminalBlock>
		</div>

		<!-- Agent prompt panel -->
		<div class="reveal reveal-delay-4 mt-12 mx-auto max-w-2xl electron-border scroll-sheen neon-hover rounded-2xl border border-border bg-surface p-6">
			<p class="font-mono text-xs text-ghost tracking-wider mb-3">Tell your agent:</p>
			<div class="flex items-center gap-3 rounded-lg border border-slate-mid bg-abyss px-4 py-3">
				<code class="flex-1 font-mono text-sm text-neon-cyan select-all">{agentPrompt}</code>
				<button
					type="button"
					class="copy-btn shrink-0 min-w-[4.5rem] text-center rounded border border-slate-mid px-2.5 py-1.5 font-mono text-xs text-ghost hover:border-neon-cyan-dim hover:text-neon-cyan transition-all"
					data-copy={agentPrompt}
				>
					Copy
				</button>
			</div>
		</div>
	</div>
</section>

<script>import '../../../scripts/copy-btn';</script>
