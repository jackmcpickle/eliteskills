---
export const prerender = false;

const cookies = Astro.request.headers.get('cookie') ?? '';
const match = cookies.match(/account_key=([^;]+)/);
const accountKey = match?.[1]?.trim();

if (accountKey) {
	return Astro.redirect(`/account/${accountKey}`);
}

return Astro.redirect('/login');
---
